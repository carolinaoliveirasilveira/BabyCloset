server:
  port: 8080

spring:
  application:
    name: wardrobe-service

  datasource:
    url: jdbc:postgresql://postgres-wardrobe:5432/wardrobe_db
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  kafka:
    bootstrap-servers: kafka:9092

logging:
  level:
    org.springframework.kafka: info
