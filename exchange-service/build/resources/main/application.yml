server:
  port: 0

spring:
  application:
    name: exchange-service

  datasource:
    url: jdbc:postgresql://localhost:5432/babycloset
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  kafka:
    bootstrap-servers: localhost:9092

logging:
  level:
    org.springframework.kafka: info
